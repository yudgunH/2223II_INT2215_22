#include<iostream>
#include<cmath>
using namespace std;
int BinarySortInsertion(a[] ,int n){
    
}
int main()
{
    int n; cin >> n;
    int tmp = n;
    float Male[n];
    float feMale[n];
    int i = 0;
    while (tmp)
    {
        cin >> Male[i] >> feMale[i] ;
        tmp--;
        i++;
    }
    sort(Male, Male + n);
    sort(feMale, feMale + n);
    
    return 0;
} // namespace std;

